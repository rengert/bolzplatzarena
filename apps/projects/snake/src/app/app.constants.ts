export enum Level {
  Easy,
  Normal,
  Hard,
  Faster,
}

export enum Direction {
  Left = 37,
  Up = 38,
  Right = 39,
  Down = 40,
}

export enum Speed {
  Slow = 700,
  Medium = 250,
  Fast = 100,
}
